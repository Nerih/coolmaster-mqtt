{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "docker-build",
      "type": "shell",
      "command": "docker build -t coolmaster-mqtt-bridge .",
      "options": {
        "shell": {
          "executable": "/bin/bash",
          "args": ["-c"]
        }
      },
      "problemMatcher": []
    },
    {
      "label": "docker-run",
      "type": "shell",
      "command": "docker run --rm -it --env-file .env -v \"${workspaceFolder}\":/app -w /app --network=host coolmaster-mqtt-bridge",
      "dependsOn": ["docker-build"],
      "options": {
        "shell": {
          "executable": "/bin/bash",
          "args": ["-c"]
        }
      },
      "problemMatcher": []
    }
  ]
}
